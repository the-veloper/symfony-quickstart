{% extends 'base.html.twig' %}
{% block title %}{{ product.name }}{% endblock %}
{% block body %}
    <a href="{{ path('app_index_index') }}"> &laquo;Back </a>
    <div class="product-title">
        <h1>{{ product.name }}</h1>
    </div>

   <!-- <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ product.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ product.name }}</td>
        </tr>
        <tr>
            <th>Category:</th>
            <td>Plod i zelen 4uk</td>
        </tr>
          <tr>
            <th>Description</th>
            <td>{{ product.description }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ product.price }}</td>
        </tr>
        <tr>
            <th>Stockqty</th>
            <td>{{ product.instock }}</td>
        </tr>
        <tr>
            <th></th>
            <td><img src="{{ vich_uploader_asset(product, 'imageFile') }}" alt="{{ product.name }}" /></td>
        </tr>
        <tr>
            <th>Categoryid</th>
            <td>{{ product.categoryId }}</td>
        </tr>
        </tbody>
    </table>-->

    <div class="row product">
        <div class="col-xs-12 col-sm-4 col-md-6 col-lg-6">
            <img src="{{ vich_uploader_asset(product, 'imageFile') }}" />
        </div>
        <div class="col-xs-12 col-sm-8 col-md-6 col-lg-6">
            <div class="row">
                <p>
                    <b>Category:</b> {{ product.categoryid }}
                </p>
            </div>
            <div class="row">
                <p>
                    <b>Description:</b><br>
                    {{ product.description }}
                </p>
            </div>
        </div>
    </div>
    <hr>
    <div class="row">
            <div class="col-md-12">
                <p>Price: {{ product.price }}$</p>
                <p>Remaining: {{ product.instock }}</p>
                <button id="add_to_card" class="btn btn-success">Add to cart</button>
                <a href="{{ path('product_buy', { 'id': product.id }) }}" class="btn btn-warning">Buy</a>
            </div>
    </div>
    <ul>
        <li>
            <a href="{{ path('app_index_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cart_add', { 'id': product.id }) }}">Add to card</a>
        </li>
        <li>
            <a href="{{ path('product_buy', { 'id': product.id }) }}">Buy</a>
        </li>
        {% if is_granted('ROLE_EDITOR') %}
        <li>
            <a href="{{ path('product_edit', { 'id': product.id }) }}" class="btn btn-warning">Edit</a>
        </li>

        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
        {% endif %}
    </ul>
{% endblock %}
